export default function HomeTemplate({ leftChild, rightChild }) {
    return (
      <div>
        <div>Left child: {leftChild}</div>
        <div>Right child: {rightChild}</div>
      </div>
    );
  }